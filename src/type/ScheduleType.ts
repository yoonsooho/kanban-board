export type PostSchedulesType = {
    title: string;
    startDate: string;
    endDate?: string;
};
export type GetSchedulesType = {
    id: string;
    title: string;
    startDate: string;
    endDate?: string;
};
